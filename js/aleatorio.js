const nomes = [ "Luiza", "Graziella", "Felipe", "Magali", "Maranhão", "Alexandre"];

function aleaorio (lista){
    const posicao = Math.floor(Math.random()* lista.lenght;
    return lista[posicao];
}

export const nome = aleatorio (nomes)
